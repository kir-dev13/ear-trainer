const InputAudioFile = () => {
    return (
        <input
            id="input"
            type="file"
            onChange={() => {
                // loadFile();
            }}
        />
    );
};

export default InputAudioFile;
